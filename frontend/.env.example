# Leave blank for local dev (Vite proxy handles it).
# In production (e.g. Render), set this to your deployed backend URL + /api
# Example: VITE_API_URL=https://kartiq-api.onrender.com/api
VITE_API_URL=
